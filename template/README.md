# Store link template

Copy this folder, edit `menu.json`, set `.env`, then `npm start` or deploy with Vercel.

See repo root README and `skills/scan-order-review/SKILL.md` for one-shot deploy instructions.
